#pragma once

#include <GyverDB.h>

DB_KEYS(db,
    storage_type,
    lcd_brightness,
    audio_volume,
    audio_player_shuffle,
    audio_player_loop,
    n_lcd_brightness,
    n_audio_volume,
    reboot_to_msc
);
